//- Sección para widgets que identifica si esta vacía
//- @author zkreations
//- @param {string} id - Identificador único

mixin section(id)
  //- Etiqueta section que comprueba si contiene widgets visibles
  b:section(id=id cond=`data:widgets any (w => w.sectionId == "${id}")`)&attributes(attributes)
    //- Contenido (b:widget)
    if block
      block