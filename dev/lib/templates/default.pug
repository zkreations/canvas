<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE html>
include ../../lib/init
block root

html&attributes(htmlAttr)
  //
    #{"\n"}Canvas v#{canvas.Version} | github.com/zkreations/canvas
  head
    b:include(name="canvas:head")
    //- Estilos del tema (obligatorio pero se puede deshabiliar)
    +skin
      block skin

    block head

    +defaultmarkups(false)
      block defaultmarkups

  body
    block body